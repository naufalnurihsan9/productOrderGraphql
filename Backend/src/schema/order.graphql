type Query {
  orders: [Order!]!
}

type Order {
  id: Int
  transcode: String
  created: String
}

type Mutation {
  getDetailOrders(id: Int): Order
  CreateOrders(productid: Int, quantity: Int, price: Int, Order_id: Int): Message
}

type Message {
  message: String
}
